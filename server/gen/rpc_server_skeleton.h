/*
 * AUTO-GENERATED FILE. DO NOT EDIT.
 * Generated by: tools/gen_rpc_server.py
 * 内容：
 *  1) 每个 method 的参数解析辅助函数声明；
 *  2) dispatch 声明（rpc_server_dispatch）。
 */

#pragma once

#include <stdint.h>
#include <stdbool.h>
#include <inttypes.h>
#include "cJSON.h"

// 每个 *_impl 由业务代码实现；解析辅助函数由生成器提供。

// 解析 params 为 ping 所需参数，成功返回 true。字符串参数输出为指向 cJSON 内部缓冲区的指针，需保证 cJSON 生命周期。
bool rpc_parse_params_ping(cJSON* params);

// 解析 params 为 echo_i64 所需参数，成功返回 true。字符串参数输出为指向 cJSON 内部缓冲区的指针，需保证 cJSON 生命周期。
bool rpc_parse_params_echo_i64(cJSON* params, int64_t* out_x);

// 解析 params 为 add_i32 所需参数，成功返回 true。字符串参数输出为指向 cJSON 内部缓冲区的指针，需保证 cJSON 生命周期。
bool rpc_parse_params_add_i32(cJSON* params, int32_t* out_a, int32_t* out_b);

// 解析 params 为 mul_double 所需参数，成功返回 true。字符串参数输出为指向 cJSON 内部缓冲区的指针，需保证 cJSON 生命周期。
bool rpc_parse_params_mul_double(cJSON* params, double* out_a, double* out_b);

// 解析 params 为 is_even 所需参数，成功返回 true。字符串参数输出为指向 cJSON 内部缓冲区的指针，需保证 cJSON 生命周期。
bool rpc_parse_params_is_even(cJSON* params, int32_t* out_n);

// 解析 params 为 strlen_s 所需参数，成功返回 true。字符串参数输出为指向 cJSON 内部缓冲区的指针，需保证 cJSON 生命周期。
bool rpc_parse_params_strlen_s(cJSON* params, const char** out_s);

// 解析 params 为 mix3 所需参数，成功返回 true。字符串参数输出为指向 cJSON 内部缓冲区的指针，需保证 cJSON 生命周期。
bool rpc_parse_params_mix3(cJSON* params, int32_t* out_a, double* out_b, bool* out_ok);

// 由生成器提供的 dispatch：解析请求 JSON，调用对应 *_impl，并返回响应 JSON 字符串（需 free）。
char* rpc_server_dispatch(const char* req_json);

